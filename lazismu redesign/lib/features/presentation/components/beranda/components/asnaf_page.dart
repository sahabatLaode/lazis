import 'package:flutter/material.dart';

class AsnafPage extends StatelessWidget {
  const AsnafPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Asnaf'),
      ),
    );
  }
}
