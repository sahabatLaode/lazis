import 'package:flutter/material.dart';

class AsnafWidget extends StatelessWidget {
  const AsnafWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Asnaf'),
      ),
    );
  }
}
